// Package extensionpb contains the generated gRPC types for the extension
// protocol. Regenerate after editing extension.proto with `go generate ./...`
// (or `make generate`); the directive below runs buf via the tool versions
// pinned in go.mod.
package extensionpb

//go:generate go tool github.com/bufbuild/buf/cmd/buf generate
