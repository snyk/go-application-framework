package auth

import (
	"testing"
)

func Test_Authenticate(t *testing.T) {
	// config := configuration.New()
	// networkAccess := networking.NewNetworkAccess(config)
	// httpClient := networkAccess.GetHttpClient()
	// token, err := Authenticate(httpClient, true)
	// assert.Nil(t, err)
	// fmt.Println(token)
}
