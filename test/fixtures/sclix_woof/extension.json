{
    "name": "sclix_woof",
    "version": "0.1.0",
    "description": "patch ascii art",
    "command": {
        "name": "woof",
        "description": "output patch ascii art",
        "options": [{
            "name": "lang",
            "shorthand": "l",
            "type": "string",
            "description": "the language you want to woof in",
            "required": false,
            "default": "default-lang"
        }]
    }
}
